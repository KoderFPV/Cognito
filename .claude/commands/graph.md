Read the file `agents/graph/chatGraph.ts` and draw the LangGraph workflow as ASCII art.

Show:
- All nodes defined with `.addNode()`
- All edges defined with `.addEdge()` and `.addConditionalEdges()`
- START and END points
- Conditional routing logic with branch labels
- Tools used by each node (LLM calls, external services, etc.)

Output ONLY the ASCII diagram. No explanations, no descriptions, no additional text.
